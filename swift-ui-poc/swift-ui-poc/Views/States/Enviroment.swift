/**
 @Environment — For getting system-wide values
 Reads a system-defined or parent-injected value
 You don’t manage its value — you read it from the environment
 Examples: @Environment(\.colorScheme), @Environment(\.dismiss), @Environment(\.presentationMode)
 */

